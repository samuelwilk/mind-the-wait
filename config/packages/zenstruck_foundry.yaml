when@dev: &dev
    # See full configuration: https://symfony.com/bundles/ZenstruckFoundryBundle/current/index.html#full-default-bundle-configuration
    zenstruck_foundry:
        persistence:
            # Flush only once per call of `PersistentObjectFactory::create()`
            flush_once: true

when@test: *dev
    # Database reset is handled by DAMA\DoctrineTestBundle\PHPUnit\PHPUnitExtension
    # which is configured in phpunit.dist.xml
